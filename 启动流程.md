# 灵境 Oracle 启动流程

下次打开项目时，在 PowerShell 里执行：

```powershell
cd "C:\Users\sanau\Documents\Codex\2026-05-29\claude-code-10-ui-ux-ios"
```

查看依赖是否存在：

```powershell
dir node_modules
```

如果没有 `node_modules`，先安装：

```powershell
npm install
```

然后启动：

```powershell
npm run dev
```

终端会显示本地地址，通常是：

```text
http://localhost:3000
```

如果 3000 被占用，Next.js 可能会显示 `http://localhost:3001`、`http://localhost:3010` 等，以终端显示的地址为准。

常见问题：

- 如果提示找不到 `package.json`，说明当前不在项目目录，先执行上面的 `cd`。
- 如果关闭终端，网站会停止运行；下次重新执行 `npm run dev` 即可。
- 如果 PowerShell 提示 npm 脚本权限问题，可以使用：

```powershell
npm.cmd run dev
```
